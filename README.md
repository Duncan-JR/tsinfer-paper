# tsinfer-paper
Evaluation/plotting code for the tsinfer 1.0 release paper

## Notebook Imports
Install once in editable mode from the repo root:

```bash
python3 -m pip install -e .
```

Then in notebooks (including those under `notebooks/`) you can import directly:

```python
from lib import likelihoods as lik
from lib import visualisation
```
