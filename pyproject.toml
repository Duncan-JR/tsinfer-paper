[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tsinfer-paper"
version = "0.0.0"
description = "Evaluation and plotting code for tsinfer paper workflows"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["lib"]
